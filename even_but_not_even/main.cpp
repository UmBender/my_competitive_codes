#include <bits/stdc++.h>

using namespace std;

int main (int argc, char *argv[])
{
	int i = 0;
	cout << i << endl;
	int valores[i][i];

	for (size_t x = 0; x < i; x++) {
		for (size_t y = 0; y < i; y++) {
			valores[y][x] = (y+1)+(x+1);


		}
		
	}
	return 0;
}
